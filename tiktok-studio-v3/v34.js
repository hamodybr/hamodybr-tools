import {
  Input, Output, Conversion, ALL_FORMATS, BlobSource, Mp4OutputFormat, BufferTarget,
  EncodedPacketSink, EncodedVideoPacketSource, EncodedAudioPacketSource,
  Quality, canEncodeVideo
} from 'https://cdn.jsdelivr.net/npm/mediabunny@1.56.3/+esm';

const $=s=>document.querySelector(s);
const fileEl=$('#file'),run=$('#run'),stop=$('#stop'),reset=$('#reset'),bar=$('#bar'),status=$('#status'),meta=$('#meta'),analysis=$('#analysis'),result=$('#result'),resultText=$('#resultText'),diag=$('#diag'),share=$('#share'),download=$('#download'),beforeReport=$('#beforeReport'),afterReport=$('#afterReport'),copyBefore=$('#copyBefore'),copyAfter=$('#copyAfter'),copyBoth=$('#copyBoth'),qualityPanel=$('#qualityPanel'),qualityWarning=$('#qualityWarning'),cap=$('#cap');
const outputSize=$('#outputSize'),outputFps=$('#outputFps'),bitrate=$('#bitrate'),fit=$('#fit'),brightness=$('#brightness'),contrast=$('#contrast'),saturation=$('#saturation'),smoothing=$('#smoothing');

let inputFile=null,input=null,videoTrack=null,audioTrack=null,sourceFps=NaN,sourceDuration=NaN,sourceFrames=NaN,sourceW=0,sourceH=0,sourceColor=null,videoCodec=null,audioCodec=null,videoConfig=null,audioConfig=null,rotation=0,currentRun=false,currentConversion=null,outputBlob=null,outputName='',objectUrl=null,beforeText='',afterText='',fileToken=0,codecReady=false,wakeLock=null;

const bytes=n=>n<1024?n+' B':n<1048576?(n/1024).toFixed(1)+' KB':(n/1048576).toFixed(2)+' MB';
const time=s=>Number.isFinite(s)?`${s.toFixed(3)} s`:'Unknown';
const selectedMode=()=>document.querySelector('input[name="mode"]:checked')?.value||'losslessTiming';
const even=n=>Math.max(2,Math.round(n/2)*2);
const colorValue=(obj,...keys)=>{for(const key of keys){if(obj?.[key]!=null)return String(obj[key])}return''};
function colorLabel(){const p=colorValue(sourceColor,'primaries','colorPrimaries'),t=colorValue(sourceColor,'transfer','transferCharacteristics');const raw=(p+' '+t).toLowerCase(),hdr=/2020|hlg|arib|pq|2084/.test(raw);return `${hdr?'HDR':'SDR'}${t?' • '+t:''}`}
function sourceIsHdr(){return colorLabel().startsWith('HDR')}
function setStatus(msg,p){status.textContent=msg;if(Number.isFinite(p))bar.style.width=Math.max(0,Math.min(100,p))+'%'}

let miQueue=Promise.resolve();
const mediaInfoReady=window.MediaInfo.mediaInfoFactory({format:'text',full:true,coverData:false,chunkSize:1024*1024,locateFile:(path,prefix)=>path==='MediaInfoModule.wasm'?'https://cdn.jsdelivr.net/npm/mediainfo.js@0.3.7/dist/MediaInfoModule.wasm':`${prefix}${path}`});
function inspectMedia(blob,name){const job=async()=>{const mi=await mediaInfoReady;const readChunk=async(chunkSize,offset)=>new Uint8Array(await blob.slice(offset,offset+chunkSize).arrayBuffer());const raw=await mi.analyzeData(blob.size,readChunk);return `HAMODYBR Video Inspector\nFile: ${name}\nFile size: ${bytes(blob.size)}\n\n${raw}`};miQueue=miQueue.catch(()=>{}).then(job);return miQueue}
async function copyText(text,button){if(!text)return;try{await navigator.clipboard.writeText(text);const old=button.textContent;button.textContent='Copied ✓';setTimeout(()=>button.textContent=old,1200)}catch{}}
copyBefore.onclick=()=>copyText(beforeText,copyBefore);
copyAfter.onclick=()=>copyText(afterText,copyAfter);
copyBoth.onclick=()=>copyText(`===== BEFORE =====\n${beforeText}\n\n===== AFTER =====\n${afterText}`,copyBoth);

function setPreset(mode){const cells=[...document.querySelectorAll('.preset div')];const rows=mode==='losslessTiming'?[['Video payload','Packet copy • unchanged'],['Audio payload','Packet copy • unchanged'],['Timing','×2 timestamps + durations'],['FPS target','~60 → ~30'],['Duration target','~2× source'],['Lossless Guard','bytes + hash + frame count']]:mode==='losslessRemux'?[['Video payload','Packet copy • unchanged'],['Audio payload','Packet copy • unchanged'],['Timing','Original timing'],['FPS target','Source FPS'],['Duration target','Source duration'],['Lossless Guard','bytes + hash + frame count']]:[['Video path','Hardware H.264 re-encode'],['Audio path','Copy when compatible'],['Adjustments','All OFF by default'],['Frame rate','Original by default'],['Output size','Original by default'],['Inspector','Before + after report']];cells.forEach((cell,i)=>{cell.querySelector('b').textContent=rows[i][0];cell.querySelector('span').textContent=rows[i][1]})}
function refreshMode(){const mode=selectedMode();document.querySelectorAll('.modeCard').forEach(card=>card.classList.toggle('active',card.querySelector('input').checked));qualityPanel.hidden=mode!=='qualityLab';setPreset(mode);run.textContent=mode==='losslessTiming'?'Build V3.4 lossless timing file':mode==='losslessRemux'?'Build lossless remux':'Build optional Quality Lab file';refreshRun()}
document.querySelectorAll('input[name="mode"]').forEach(el=>el.addEventListener('change',refreshMode));
[outputSize,outputFps,bitrate,fit,brightness,contrast,saturation,smoothing].forEach(el=>el.addEventListener('change',refreshRun));

function refreshRun(){const mode=selectedMode(),hasSource=!!(inputFile&&videoTrack&&videoCodec&&videoConfig&&Number.isFinite(sourceFps)),timingOk=sourceFps>=55&&sourceFps<=65,engineOk=mode!=='qualityLab'||codecReady;run.disabled=!(hasSource&&engineOk&&!currentRun&&(mode!=='losslessTiming'||timingOk));if(!hasSource)return;analysis.hidden=false;if(mode==='losslessTiming'){if(timingOk){analysis.className='note ok';analysis.innerHTML=`<b>Recommended Lossless Timing ready ✓</b> ${sourceFps.toFixed(3)} FPS → ${(sourceFps/2).toFixed(3)} FPS timing. Encoded video/audio payloads remain unchanged.`}else{analysis.className='note bad';analysis.innerHTML=`<b>Lossless Timing expects a ~60 FPS source.</b> Detected ${sourceFps.toFixed(3)} FPS. Choose Lossless Remux or Quality Lab for this file.`}}else if(mode==='losslessRemux'){analysis.className='note ok';analysis.innerHTML=`<b>Lossless Remux ready ✓</b> Original ${sourceFps.toFixed(3)} FPS and duration will be preserved while the MP4 container is rebuilt.`}else{const adjustments=[];if(outputSize.value!=='original')adjustments.push('1080 output');if(outputFps.value!=='original')adjustments.push(outputFps.value+' FPS');if(brightness.value!=='1')adjustments.push('brightness');if(contrast.value!=='1')adjustments.push('contrast');if(saturation.value!=='1')adjustments.push('saturation');if(smoothing.value!=='0')adjustments.push('smoothing');analysis.className='note warn';analysis.innerHTML=`<b>Quality Lab will re-encode the video.</b> ${adjustments.length?'Enabled: '+adjustments.join(', ')+'.':'No visual adjustments enabled; this is a compatibility re-encode.'}${sourceIsHdr()?' HDR input detected: use a lossless mode to preserve its original HDR payload.':''}`;qualityWarning.innerHTML=sourceIsHdr()?'<b>HDR source detected.</b> Quality Lab may alter HDR. Lossless Timing or Lossless Remux is recommended. SDR→HDR conversion is not faked by metadata.':'<b>All adjustments are OFF by default.</b> This source is SDR. SDR→HDR conversion is intentionally not faked by metadata.'}}

async function capability(){try{const q=new Quality({bitrate:20e6,bitrateMode:'constant'});const [ok]=await Promise.all([canEncodeVideo('avc',{width:1080,height:1920,quality:q,hardwareAcceleration:'prefer-hardware'}),mediaInfoReady]);codecReady=!!ok;if(!ok)throw new Error('Hardware H.264 encoder unavailable at 1080×1920');cap.className='note ok';cap.innerHTML='<b>Engines ready ✓</b> Lossless packet copy, MediaInfo and optional hardware H.264 are available.'}catch(e){codecReady=false;cap.className='note warn';cap.innerHTML='<b>Lossless modes are ready.</b> Optional Quality Lab is unavailable on this browser: '+(e?.message||e)}finally{refreshRun()}}
capability();

function hashInit(){return{h:0xcbf29ce484222325n,total:0,packets:0}}
function hashFeed(state,data){let h=state.h;for(let i=0;i<data.length;i++){h^=BigInt(data[i]);h=BigInt.asUintN(64,h*0x100000001b3n)}state.h=h;state.total+=data.length;state.packets++}
function hashText(state){return state.h.toString(16).padStart(16,'0')}
async function scanTrack(track){const sink=new EncodedPacketSink(track),state=hashInit();for await(const p of sink.packets())hashFeed(state,p.data);return state}

fileEl.onchange=async()=>{const f=fileEl.files?.[0];if(!f)return;const token=++fileToken;inputFile=f;input=new Input({formats:ALL_FORMATS,source:new BlobSource(f)});videoTrack=await input.getPrimaryVideoTrack();audioTrack=await input.getPrimaryAudioTrack();if(!videoTrack){setStatus('No video track found.',0);return}beforeText=afterText='';beforeReport.textContent='Reading full MediaInfo report…';afterReport.textContent='The output report will appear after processing.';copyBefore.disabled=copyAfter.disabled=copyBoth.disabled=true;result.classList.remove('show');meta.hidden=false;['name','codec','fps','frames','size','color','duration'].forEach(id=>$('#'+id).textContent=id==='name'?f.name:'Analyzing…');setStatus('Analyzing source packets, dimensions and color…',5);try{const [metrics,dur,stats,w,h,cs,vCodec,vCfg,rot,aCodec,aCfg]=await Promise.all([videoTrack.computeFrameRateMetrics({targetPacketCount:256}),videoTrack.computeDuration(),videoTrack.computePacketStats(),videoTrack.getDisplayWidth(),videoTrack.getDisplayHeight(),videoTrack.getColorSpace().catch(()=>null),videoTrack.getCodec(),videoTrack.getDecoderConfig(),videoTrack.getRotation(),audioTrack?audioTrack.getCodec():Promise.resolve(null),audioTrack?audioTrack.getDecoderConfig():Promise.resolve(null)]);if(token!==fileToken)return;sourceFps=metrics.bestGuessFrameRate;sourceDuration=dur;sourceFrames=stats.packetCount;sourceW=w;sourceH=h;sourceColor=cs;videoCodec=vCodec;videoConfig=vCfg;rotation=rot;audioCodec=aCodec;audioConfig=aCfg;$('#codec').textContent=videoCodec||'Unknown';$('#fps').textContent=sourceFps.toFixed(3)+' FPS';$('#frames').textContent=String(sourceFrames);$('#size').textContent=`${sourceW} × ${sourceH}`;$('#color').textContent=colorLabel();$('#duration').textContent=time(sourceDuration);refreshRun();setStatus('Source ready. MediaInfo report loading…',0);inspectMedia(f,f.name).then(text=>{if(token!==fileToken)return;beforeText=text;beforeReport.textContent=text;copyBefore.disabled=false;copyBoth.disabled=!(beforeText&&afterText)}).catch(e=>beforeReport.textContent='MediaInfo failed: '+(e?.message||e))}catch(e){analysis.hidden=false;analysis.className='note bad';analysis.textContent='Source analysis failed: '+(e?.message||e);setStatus('Source analysis failed.',0)}};

reset.onclick=()=>{if(currentRun)return;++fileToken;fileEl.value='';inputFile=input=videoTrack=audioTrack=null;sourceFps=sourceDuration=sourceFrames=NaN;sourceW=sourceH=0;sourceColor=null;videoCodec=audioCodec=videoConfig=audioConfig=null;beforeText=afterText='';meta.hidden=true;analysis.hidden=true;result.classList.remove('show');bar.style.width='0';beforeReport.textContent='Choose a video to generate the full report.';afterReport.textContent='The output report will appear after processing.';copyBefore.disabled=copyAfter.disabled=copyBoth.disabled=true;status.textContent='Choose an MP4/MOV source.';if(objectUrl){URL.revokeObjectURL(objectUrl);objectUrl=null}refreshRun()};
stop.onclick=async()=>{if(!currentConversion)return;stop.disabled=true;setStatus('Stopping safely…');try{await currentConversion.cancel()}catch{}};
async function holdWake(){try{if('wakeLock'in navigator)wakeLock=await navigator.wakeLock.request('screen')}catch{}}
async function releaseWake(){try{await wakeLock?.release()}catch{}wakeLock=null}

async function verifiedOutputMetrics(blob){const outInput=new Input({formats:ALL_FORMATS,source:new BlobSource(blob)}),v=await outInput.getPrimaryVideoTrack(),a=await outInput.getPrimaryAudioTrack();if(!v)throw new Error('Output video track missing');const [vStats,vDur,vMetrics,vCodec,vHash,aStats,aDur,aCodec,aHash]=await Promise.all([v.computePacketStats(),v.computeDuration(),v.computeFrameRateMetrics({targetPacketCount:256}),v.getCodec(),scanTrack(v),a?a.computePacketStats():Promise.resolve(null),a?a.computeDuration():Promise.resolve(NaN),a?a.getCodec():Promise.resolve(null),a?scanTrack(a):Promise.resolve(null)]);return{vStats,vDur,vMetrics,vCodec,vHash,aStats,aDur,aCodec,aHash}}
async function basicOutputMetrics(blob){const outInput=new Input({formats:ALL_FORMATS,source:new BlobSource(blob)}),v=await outInput.getPrimaryVideoTrack();if(!v)throw new Error('Output video track missing');const [stats,duration,metrics,codec,w,h]=await Promise.all([v.computePacketStats(),v.computeDuration(),v.computeFrameRateMetrics({targetPacketCount:256}),v.getCodec(),v.getDisplayWidth(),v.getDisplayHeight()]);return{stats,duration,metrics,codec,w,h}}
async function saveReady(blob,name,text,details){outputBlob=blob;outputName=name;resultText.textContent=text;diag.textContent=details;result.classList.add('show');const sf=new File([blob],name,{type:'video/mp4'});share.hidden=!(navigator.share&&navigator.canShare?.({files:[sf]}));share.onclick=async()=>{try{await navigator.share({files:[sf],title:'HAMODYBR Studio V3.4'})}catch{}};download.onclick=()=>{if(objectUrl)URL.revokeObjectURL(objectUrl);objectUrl=URL.createObjectURL(blob);const a=document.createElement('a');a.href=objectUrl;a.download=name;document.body.appendChild(a);a.click();a.remove()}}

async function runLossless(scale){setStatus('Stage 1/5 — Hashing original packet payloads…',8);const [srcVHash,srcAHash]=await Promise.all([scanTrack(videoTrack),audioTrack?scanTrack(audioTrack):Promise.resolve(null)]);setStatus('Stage 2/5 — Creating MP4 packet-copy output…',22);const target=new BufferTarget(),output=new Output({format:new Mp4OutputFormat({fastStart:'in-memory'}),target});const vSource=new EncodedVideoPacketSource(videoCodec);output.addVideoTrack(vSource,{rotation,frameRate:sourceFps/scale});let aSource=null;if(audioTrack&&audioCodec&&audioConfig){aSource=new EncodedAudioPacketSource(audioCodec);output.addAudioTrack(aSource)}await output.start();const pipeVideo=(async()=>{const sink=new EncodedPacketSink(videoTrack);let first=true,count=0;for await(const p of sink.packets()){const q=p.clone({timestamp:p.timestamp*scale,duration:p.duration*scale});await vSource.add(q,first?{decoderConfig:videoConfig}:undefined);first=false;count++;if(count%120===0)setStatus(`Stage 3/5 — Copying video packets ${count}/${sourceFrames}…`,25+Math.min(45,(count/sourceFrames)*45))}vSource.close();return count})();const pipeAudio=(async()=>{if(!aSource||!audioTrack)return 0;const sink=new EncodedPacketSink(audioTrack);let first=true,count=0;for await(const p of sink.packets()){const q=p.clone({timestamp:p.timestamp*scale,duration:p.duration*scale});await aSource.add(q,first?{decoderConfig:audioConfig}:undefined);first=false;count++}aSource.close();return count})();const [writtenVideo,writtenAudio]=await Promise.all([pipeVideo,pipeAudio]);await output.finalize();if(!target.buffer)throw new Error('Muxer returned no MP4 buffer');setStatus('Stage 4/5 — Verifying packet identity…',76);const blob=new Blob([target.buffer],{type:'video/mp4'}),out=await verifiedOutputMetrics(blob),videoBytesMatch=out.vHash.total===srcVHash.total,videoHashMatch=hashText(out.vHash)===hashText(srcVHash),videoFramesMatch=out.vStats.packetCount===sourceFrames,audioBytesMatch=!srcAHash||out.aHash?.total===srcAHash.total,audioHashMatch=!srcAHash||hashText(out.aHash)===hashText(srcAHash),durationRatio=out.vDur/sourceDuration,fpsTarget=sourceFps/scale,fpsOk=Math.abs(out.vMetrics.bestGuessFrameRate-fpsTarget)<0.05;if(!(videoBytesMatch&&videoHashMatch&&videoFramesMatch&&audioBytesMatch&&audioHashMatch))throw new Error('Lossless Guard failed: encoded packet payload changed');const base=inputFile.name.replace(/\.[^.]+$/,'')||'video',suffix=scale===2?`lossless-${Math.round(sourceFps)}to${Math.round(fpsTarget)}`:'lossless-remux',name=`${base}-hamodybr-v34-${suffix}.mp4`;const details=`Mode: ${scale===2?'lossless packet-copy retime ×2':'lossless packet-copy remux'}\nVideo codec: ${videoCodec} → ${out.vCodec}\nVideo frames: ${sourceFrames} → ${out.vStats.packetCount} ${videoFramesMatch?'✓':''}\nVideo payload bytes: ${srcVHash.total} → ${out.vHash.total} ${videoBytesMatch?'✓':''}\nVideo payload hash: ${hashText(srcVHash)} → ${hashText(out.vHash)} ${videoHashMatch?'✓':''}\nSource FPS: ${sourceFps.toFixed(3)}\nOutput FPS: ${out.vMetrics.bestGuessFrameRate.toFixed(3)} ${fpsOk?'✓':''}\nSource duration: ${sourceDuration.toFixed(3)} s\nOutput duration: ${out.vDur.toFixed(3)} s\nDuration ratio: ${durationRatio.toFixed(6)}×\nResolution: ${sourceW} × ${sourceH} preserved\nColor profile: ${colorLabel()} preserved in encoded payload\nRotation metadata: ${rotation}°\nVideo packets written: ${writtenVideo}\nAudio codec: ${audioCodec||'none'} → ${out.aCodec||'none'}\nAudio packets written: ${writtenAudio}\nAudio payload hash match: ${audioHashMatch?'✓':'✗'}\nRe-encode: NO`;await saveReady(blob,name,`${bytes(blob.size)} • encoded video/audio payload preserved • ${out.vMetrics.bestGuessFrameRate.toFixed(3)} FPS • duration ×${durationRatio.toFixed(3)}.`,details);return{blob,name}}

async function runQuality(){await holdWake();stop.hidden=false;stop.disabled=false;const portrait=sourceH>=sourceW,outW=outputSize.value==='1080'?(portrait?1080:1920):even(sourceW),outH=outputSize.value==='1080'?(portrait?1920:1080):even(sourceH),fpsTarget=outputFps.value==='original'?sourceFps:Number(outputFps.value),reqMbps=Number(bitrate.value),q=new Quality({bitrate:reqMbps*1e6,bitrateMode:'constant'}),target=new BufferTarget(),output=new Output({format:new Mp4OutputFormat({fastStart:'in-memory'}),target}),b=Number(brightness.value),c=Number(contrast.value),s=Number(saturation.value),blur=Number(smoothing.value),filtersActive=b!==1||c!==1||s!==1||blur>0,video={width:outW,height:outH,fit:fit.value,codec:'avc',quality:q,keyFrameInterval:.5,hardwareAcceleration:'prefer-hardware',forceTranscode:true};if(outputFps.value!=='original')video.frameRate=fpsTarget;if(filtersActive){const canvas=typeof OffscreenCanvas!=='undefined'?new OffscreenCanvas(outW,outH):Object.assign(document.createElement('canvas'),{width:outW,height:outH}),ctx=canvas.getContext('2d',{alpha:false});video.process=sample=>{ctx.save();ctx.fillStyle='#000';ctx.fillRect(0,0,outW,outH);ctx.filter=`brightness(${b}) contrast(${c}) saturate(${s})${blur?` blur(${blur}px)`:''}`;sample.draw(ctx,0,0,outW,outH);ctx.restore();return canvas};video.processedWidth=outW;video.processedHeight=outH}setStatus('Stage 1/4 — Initializing optional hardware re-encode…',7);currentConversion=await Conversion.init({input,output,tracks:'primary',video,copy:{mode:'preferred'},showWarnings:false});if(!currentConversion.isValid)throw new Error('Quality Lab configuration is not valid on this device');currentConversion.onProgress=p=>setStatus(`Stage 2/4 — Encoding ${Math.round(p*100)}%`,10+p*70);await currentConversion.execute();currentConversion=null;if(!target.buffer)throw new Error('Encoder returned no MP4 buffer');setStatus('Stage 3/4 — Inspecting output…',84);const blob=new Blob([target.buffer],{type:'video/mp4'}),out=await basicOutputMetrics(blob),base=inputFile.name.replace(/\.[^.]+$/,'')||'video',name=`${base}-hamodybr-v34-quality-lab.mp4`,enabled=[outputSize.value==='1080'?'1080 output':null,outputFps.value!=='original'?outputFps.value+' FPS':null,b!==1?`brightness ${Math.round((b-1)*100)}%`:null,c!==1?`contrast ${Math.round((c-1)*100)}%`:null,s!==1?`saturation ${Math.round((s-1)*100)}%`:null,blur?`smoothing ${blur}px`:null].filter(Boolean);const details=`Mode: Quality Lab • optional re-encode\nSource: ${sourceW} × ${sourceH} • ${sourceFps.toFixed(3)} FPS • ${colorLabel()}\nOutput: ${out.w} × ${out.h} • ${out.metrics.bestGuessFrameRate.toFixed(3)} FPS\nCodec: ${videoCodec} → ${out.codec}\nBitrate request: ${reqMbps} Mb/s\nFit: ${fit.value}\nAdjustments: ${enabled.length?enabled.join(', '):'none'}\nFast Start: yes\nHDR generation: NO\nRe-encode: YES`;await saveReady(blob,name,`${bytes(blob.size)} • ${out.w}×${out.h} • ${out.metrics.bestGuessFrameRate.toFixed(3)} FPS • optional re-encode complete.`,details);return{blob,name}}

run.onclick=async()=>{if(run.disabled||currentRun)return;currentRun=true;refreshRun();result.classList.remove('show');afterText='';afterReport.textContent='Processing…';copyAfter.disabled=true;copyBoth.disabled=true;try{const mode=selectedMode(),made=mode==='qualityLab'?await runQuality():await runLossless(mode==='losslessTiming'?2:1);setStatus('Generating AFTER MediaInfo report…',93);const outFile=new File([made.blob],made.name,{type:'video/mp4'});afterText=await inspectMedia(outFile,made.name);afterReport.textContent=afterText;copyAfter.disabled=false;copyBoth.disabled=!(beforeText&&afterText);setStatus(mode==='qualityLab'?'Done. Quality Lab output and reports are ready.':'Done. Lossless Guard passed.',100)}catch(e){console.error(e);afterReport.textContent='Output was not completed.';setStatus((e?.name==='ConversionCanceledError'?'Stopped.':'V3.4 failed: '+(e?.message||e)),0)}finally{currentConversion=null;currentRun=false;stop.disabled=true;stop.hidden=true;await releaseWake();refreshRun()}};

refreshMode();
